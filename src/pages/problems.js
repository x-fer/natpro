import React from 'react';
import Layout from '@theme/Layout';

function Problems() {
  return (
    <Layout title="Problems">
      <div
        style={{
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          height: '50vh',
          fontSize: '20px',
        }}>
        <p>
          Edit <code>pages/problems.js</code> and save to reload.
        </p>
      </div>
    </Layout>
  );
}

export default Problems;